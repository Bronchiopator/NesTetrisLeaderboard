import { TransformToRowPipe } from './transform-to-row.pipe';

describe('TransformToRowPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformToRowPipe();
    expect(pipe).toBeTruthy();
  });
});
